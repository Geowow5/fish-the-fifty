# Vercel Web Analytics update

This build adds `@vercel/analytics` to the Fish the Fifty project and renders the Analytics component from the shared Next.js root layout.

After the updated site is deployed and visited, Vercel can collect page-view and visitor data across the homepage and every state, challenge, and water-guide route.
